version: "3.9"

services:
  postgres: 
    image: postgres:16.3-alpine3.20
    restart: always
    container_name: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: root
    networks:
      - postgres-network

networks: 
  postgres-network: